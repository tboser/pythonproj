"""Smoke test — replace once the project has real tests."""

from {{ package_name }} import __version__


def test_version_is_present() -> None:
    assert isinstance(__version__, str)
    assert __version__
